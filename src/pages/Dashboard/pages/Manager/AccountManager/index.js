import classNames from "classnames/bind";
import style from "./AccountManager.module.scss";

const cx = classNames.bind(style);

function AccountManager() {
    return(
        <h1>Account Manager</h1>
    )
}

export default AccountManager;